## SECTION=staging
## old section:
### SECTION=system_libraries

config LIBOIL
	tristate
	prompt "liboil"
	select LIBC_M
	select LIBC_RT
	help
	  Liboil is a library of simple functions that are optimized for
	  various CPUs ("optimization of inner loops"). See the project
	  homepage for details:

	  http://liboil.freedesktop.org/wiki/

	  STAGING: remove in PTXdist 2023.12.0
	  Upstream dead and no longer used (it was replaced by orc in GStreamer).
